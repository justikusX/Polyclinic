<UserControl x:Class="Polyclinic.Views.VisitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Polyclinic.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Фильтры -->
        <Border Grid.Row="0" CornerRadius="5" Background="{DynamicResource CardBackgroundBrush}"
                Padding="15" Margin="0 0 0 10" Effect="{StaticResource ShadowEffect}">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <DatePicker materialDesign:HintAssist.Hint="Дата с" SelectedDate="{Binding StartDate}"
                                Style="{StaticResource MaterialDesignFloatingHintDatePicker}" Width="180"/>

                    <DatePicker materialDesign:HintAssist.Hint="Дата по" SelectedDate="{Binding EndDate}"
                                Style="{StaticResource MaterialDesignFloatingHintDatePicker}" Width="180" Margin="10 0 0 0"/>

                    <ComboBox ItemsSource="{Binding Doctors}" SelectedValue="{Binding SelectedDoctor}"
                              DisplayMemberPath="FullName" materialDesign:HintAssist.Hint="Врач"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}" Width="250" Margin="10 0 0 0"/>

                    <Button Content="Фильтровать" Command="{Binding FilterVisitsCommand}"
                            Style="{StaticResource PrimaryButton}" Margin="10 0 0 0" Width="120"/>
                </StackPanel>

                <Button Content="Записать на приём" HorizontalAlignment="Right" Margin="0 10 0 0"
                        Style="{StaticResource SuccessButton}" Width="180"
                        Command="{Binding AddVisitCommand}"/>
            </StackPanel>
        </Border>

        <!-- Список визитов -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Visits}" SelectedItem="{Binding SelectedVisit}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  Style="{StaticResource ModernDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding VisitDate, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="*"
                                   ElementStyle="{StaticResource DataGridTextBlockStyle}"/>

                <DataGridTemplateColumn Header="Врач" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="30" Height="30" CornerRadius="15" 
                                        Background="{DynamicResource PrimaryLightBrush}" Margin="0 0 5 0">
                                    <materialDesign:PackIcon Kind="Doctor" Width="16" Height="16" 
                                                            Foreground="{DynamicResource PrimaryBrush}"/>
                                </Border>
                                <TextBlock Text="{Binding Doctor.FullName}" VerticalAlignment="Center"
                                           Style="{StaticResource DataGridTextBlockStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Пациент" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="30" Height="30" CornerRadius="15" 
                                        Background="{DynamicResource SecondaryLightBrush}" Margin="0 0 5 0">
                                    <materialDesign:PackIcon Kind="Account" Width="16" Height="16" 
                                                            Foreground="{DynamicResource SecondaryBrush}"/>
                                </Border>
                                <TextBlock Text="{Binding Patient.FullName}" VerticalAlignment="Center"
                                           Style="{StaticResource DataGridTextBlockStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Диагноз" Binding="{Binding Diagnosis.Name}" Width="*"
                                   ElementStyle="{StaticResource DataGridTextBlockStyle}"/>

                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Подробнее" Style="{StaticResource FlatButton}"
                                    Command="{Binding DataContext.ShowVisitDetailsCommand, 
                                    RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        
    </Grid>
</UserControl>