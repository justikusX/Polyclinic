<UserControl x:Class="Polyclinic.Views.DoctorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Polyclinic.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель фильтров -->
        <Border Grid.Row="0" CornerRadius="5" Background="{DynamicResource CardBackgroundBrush}"
                Padding="15" Margin="0 0 0 10" Effect="{StaticResource ShadowEffect}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Doctor" Width="24" Height="24" 
                                        Foreground="{DynamicResource PrimaryBrush}" Margin="0 0 10 0"/>
                <ComboBox 
                    ItemsSource="{Binding Specialties}"
                    SelectedItem="{Binding SelectedSpecialty}"
                    materialDesign:HintAssist.Hint="Специальность"
                    Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                    Width="250"
                    Margin="10 0 0 0"/>

                <Button Content="Добавить врача" Margin="20 0 0 0" Command="{Binding AddDoctorCommand}"
                        Style="{StaticResource PrimaryButton}" Width="150"/>
            </StackPanel>
        </Border>

        <!-- Список врачей -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Doctors}" SelectedItem="{Binding SelectedDoctor}"
          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
          Style="{StaticResource ModernDataGrid}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Фото" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="40" Height="40" CornerRadius="20" 
                            Background="{DynamicResource PrimaryLightBrush}">
                                <materialDesign:PackIcon Kind="Account" Width="24" Height="24" 
                                                Foreground="{DynamicResource PrimaryBrush}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"
                           ElementStyle="{StaticResource DataGridTextBlockStyle}"/>

                <DataGridTextColumn Header="Специальность" Binding="{Binding Specialty}" Width="*"
                           ElementStyle="{StaticResource DataGridTextBlockStyle}"/>

                <DataGridTextColumn Header="Стаж" Binding="{Binding Experience}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridTextBlockStyle}">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Подробнее" Style="{StaticResource FlatButton}"
                            Command="{Binding DataContext.ShowDoctorDetailsCommand, 
                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>