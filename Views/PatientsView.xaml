<UserControl x:Class="Polyclinic.Views.PatientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Polyclinic.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        

        <!-- Панель поиска -->
        <Border Grid.Row="0" CornerRadius="5" Background="{DynamicResource CardBackgroundBrush}"
                Padding="15" Margin="0 0 0 10" Effect="{StaticResource ShadowEffect}">
            <StackPanel Orientation="Horizontal">
                <TextBox materialDesign:HintAssist.Hint="Поиск пациентов..." Width="300"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchPatientsCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <Button Content="Найти" Command="{Binding SearchPatientsCommand}"
                        Style="{StaticResource PrimaryButton}" Margin="10 0 0 0" Width="100"/>

                <Button Content="Добавить" Command="{Binding AddPatientCommand}"
                        Style="{StaticResource SuccessButton}" Margin="20 0 0 0" Width="120"/>
            </StackPanel>
        </Border>

        <!-- Список пациентов -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Patients}" SelectedItem="{Binding SelectedPatient}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  Style="{StaticResource ModernDataGrid}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Фото" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="40" Height="40" CornerRadius="20" 
                                    Background="{DynamicResource SecondaryLightBrush}">
                                <materialDesign:PackIcon Kind="Account" Width="24" Height="24" 
                                                        Foreground="{DynamicResource SecondaryBrush}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"
                                   ElementStyle="{StaticResource DataGridTextBlockStyle}"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat={}{0:dd.MM.yyyy}}" Width="*"
                                   ElementStyle="{StaticResource DataGridTextBlockStyle}"/>
                <DataGridTextColumn Header="Возраст" Binding="{Binding Age}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridTextBlockStyle}">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextWrapping" Value="NoWrap"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button ToolTip="История посещений" Style="{StaticResource IconButton}"
                                        Command="{Binding DataContext.ShowPatientHistoryCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}">
                                    <materialDesign:PackIcon Kind="History" Width="20" Height="20"/>
                                </Button>

                                <Button ToolTip="Редактировать" Style="{StaticResource IconButton}" Margin="5 0 0 0">
                                    <materialDesign:PackIcon Kind="Edit" Width="20" Height="20"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>